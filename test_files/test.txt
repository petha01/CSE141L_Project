// Hey there 
//this is just a test to test on to see 
// if assembly works

loop:          // this is 13 lines above the blqz instruction.
mov r0, 0
mov r1, 1
mov r2, -1
mov r3, 30
mov r4, 4
ld r0, r1
rsl r0, 7
and r0, r1
st r0, r3
mov r6, 3
blqz r6, loop
